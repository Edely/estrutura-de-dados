#include <stdio.h>
#include <stdlib.h>
#include "ListaDinEncad.h"

int main(){
    
    Lista *l;
    return 0;
}

li = cria_lista();

libera_lista(li);

int x = tamanho_lista(Lista* li);

int x = lista_cheia(Lista* li);

int x = lista_vazia(Lista* li);

int x = remove_lista_inicio(Lista *li);

int x = remove_lista_fim(Lista *li);
    